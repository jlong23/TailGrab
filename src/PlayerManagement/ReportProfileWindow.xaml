<Window x:Class="Tailgrab.PlayerManagement.ReportProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Report User Profile"
        Width="650" Height="500"
        FontFamily="Segoe UI"
        Language="en-US"
        WindowStartupLocation="CenterOwner"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- User ID -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <Label Content="User ID:" FontWeight="Bold"/>
            <TextBox Name="UserIdTextBox" IsReadOnly="True" />
        </StackPanel>

        <!-- Category -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <Label Content="Category:" FontWeight="Bold"/>
            <TextBox Name="CategoryTextBox" IsReadOnly="True" Text="Profile"/>
        </StackPanel>

        <!-- Report Reason -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <Label Content="Report Reason:" FontWeight="Bold"/>
            <ComboBox Name="ReportReasonComboBox" 
                      ItemsSource="{Binding ReportReasons}"
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="Value"
                      SelectedIndex="0"/>
        </StackPanel>

        <!-- Report Description -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <Label Content="Report Description (AI Evaluation):" FontWeight="Bold"/>
            <TextBox Name="ReportDescriptionTextBox" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto"
                     IsReadOnly="True"
                     MinHeight="200"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="CancelButton" Content="Cancel" Width="100" Margin="0,0,12,0" Click="CancelButton_Click"/>
            <Button Name="SubmitButton" Content="Submit" Width="100" Click="SubmitButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
